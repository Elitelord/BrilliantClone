node_modules
dist
dist-ssr
*.local

# env
.env
.env.*
!.env.example

# firebase
.firebase
firebase-debug.log
firestore-debug.log

# secrets / credentials (never commit)
*.pem
*.key
*.p12
*.p8
serviceAccount*.json
service-account*.json
*-adminsdk-*.json
*firebase-adminsdk*.json
.runtimeconfig.json
google-services.json
GoogleService-Info.plist
secrets.*
*.secret

# editor / os
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
.vscode/*
!.vscode/extensions.json

# probes
probe_*.txt
shell_probe*.txt
node_check.txt

# agent skills (installed locally; see skills-lock.json for sources)
.agents/

# claude code local state (worktrees, settings, scratch) — never commit
.claude/
